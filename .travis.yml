language: python
python:
  - "3.6"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script:
  - python3 test2.py
  - result=$?
  - if [ result == 1 ];then
      echo 'No ok';
     else 
      echo "OK";
    fi
