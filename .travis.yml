language: python
python:
  - "3.6"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script:
  - python3 main.py
  - if [ $? == 0 ];then
      exit;
    else
      python3 main.py;
    fi;
